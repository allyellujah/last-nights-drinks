// math.random function in a for loop, result needs to be popped off array, 

// I would pop the items out of the array each time you run the loop and then put then push them into a new array so that you don’t get the same object twice!